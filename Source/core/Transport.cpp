#include "Transport.h"

// Transport is currently header-only; this translation unit exists so the
// class has a stable home when transport logic grows (count-in, sync modes,
// hardware transport buttons).
